---
interface Props {
  readonly class?: string | undefined
}
const { class: className } = Astro.props
---

<span class:list={["inline-flex items-baseline gap-1.5 font-mono font-bold tracking-tight text-white", className]}>
  <span aria-hidden="true" class="font-medium text-zinc-500">//</span><span>konfig<span class="text-zinc-400">.ts</span></span>
</span>
