{
  "name": "konfig.ts",
  "version": "0.0.11",
  "private": true,
  "description": "Typesafe Kubernetes + ArgoCD configuration in TypeScript, powered by Effect.",
  "packageManager": "bun@1.4.0",
  "workspaces": [
    "packages/*",
    "apps/*",
    "examples",
    "examples/full-stack",
    "examples/full-stack/apps/*",
    "examples/full-stack/shared/*"
  ],
  "catalog": {
    "@effect/platform-node": "4.0.0-rc.111",
    "@effect/vitest": "4.0.0-rc.111",
    "@types/bun": "^1.3.14",
    "@vitest/coverage-v8": "^4.1.10",
    "effect": "4.0.0-rc.111",
    "fast-check": "^4.9.0",
    "vitest": "^4.1.10",
    "yaml": "^2.9.0"
  },
  "scripts": {
    "konfig": "bun packages/cli/src/main.ts",
    "build": "bun run --cwd packages/core build && bun run --cwd packages/env build && bun run --cwd packages/k8s build && bun run --cwd packages/external-secrets build && bun run --cwd packages/sealed-secrets build && bun run --cwd packages/sops build && bun run --cwd packages/argocd build && bun run --cwd packages/docker build && bun run --cwd packages/cli build",
    "check": "bun run --cwd packages/core check && bun run --cwd packages/env check && bun run --cwd packages/k8s check && bun run --cwd packages/external-secrets check && bun run --cwd packages/sealed-secrets check && bun run --cwd packages/sops check && bun run --cwd packages/argocd check && bun run --cwd packages/docker check && bun run --cwd packages/cli check && bun run --cwd examples check && bun run --cwd examples/full-stack check && bun run --cwd apps/website check && tsc -p scripts/tsconfig.json --noEmit",
    "test": "bun scripts/cli.ts test",
    "lint": "oxlint",
    "lint:apply": "oxlint --fix",
    "knip": "knip",
    "prepare": "effect-tsgo patch --no-typescript --oxlint",
    "test:coverage": "bun run --cwd packages/core test:coverage && bun run --cwd packages/env test:coverage && bun run --cwd packages/k8s test:coverage && bun run --cwd packages/external-secrets test:coverage && bun run --cwd packages/sealed-secrets test:coverage && bun run --cwd packages/sops test:coverage && bun run --cwd packages/argocd test:coverage && bun run --cwd packages/docker test:coverage && bun run --cwd packages/cli test:coverage && bun scripts/cli.ts badges",
    "repo": "bun scripts/cli.ts"
  },
  "devDependencies": {
    "@effect/platform-node": "catalog:",
    "@effect/tsgo": "0.36.0",
    "effect": "catalog:",
    "knip": "^6.32.0",
    "oxlint": "1.77.0",
    "oxlint-tsgolint": "^7.0.2001",
    "tsdown": "^0.22.14",
    "typescript": "^7.0.2",
    "vitest": "catalog:"
  }
}
