// @konfig.ts/cli — `effect/unstable/cli`-based entrypoint for the konfig toolchain.
// Concrete commands land in M4 (build/diff/validate), M6 (crd extract/verify,
// helm fetch), M8 (set/services/graph). Run via `bun shared/konfig/cli/src/main.ts`.
export {};
